from utils import Inputstr, mapt

test_input = """1, 1
1, 6
8, 3
3, 4
5, 5
8, 9
"""

input6 = Inputstr(6)


def manhatten_distance(p1, p2):
    """Returns the manhatten distance of the two given points. A point is
a tuple with 2 integers."""
    pass


def part1(content):
    coordinates = [mapt(int, line.split(',')) for line in content.splitlines()]

    # create grid-array

    # places coordinates in array

    # fill grid with coordinates and with 0 (0 means: does not belong
    # to any coordinates)
    return coordinates
